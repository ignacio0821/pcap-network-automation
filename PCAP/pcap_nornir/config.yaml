---
core:
  # Instructs Nornir to use the standard SimpleInventory file plugin
  runner:
    plugin: "SimpleRunner"
inventory:
  plugin: "SimpleInventory"
  options:
    host_file: "hosts.yaml"
    group_file: "groups.yaml"
    defaults_file: "defaults.yaml"